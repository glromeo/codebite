{
  "name": "esnext-server-client",
  "version": "1.0.0",
  "module": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --outdir=dist --bundle --format=esm",
    "build:watch": "esbuild src/index.ts --outdir=dist --bundle --format=esm --watch"
  },
  "devDependencies": {
    "esbuild": "^0.8.47"
  }
}
